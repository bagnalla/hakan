
data Option a =
  | None
  | Some a

data Pair a b =
  | Pair a b

data Sum a b =
  | Inl a
  | Inr b

val proj1 : a × b → a
let proj1 = λ p.
  destruct p as
  | (x, _) → x

val proj2 : a × b → b
let proj2 = λ p.
  destruct p as
  | (_, y) → y
